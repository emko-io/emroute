# Preact Counter

This page demonstrates the `counter-htm` widget — an interactive
**Preact** island that loads `htm` + `preact` from `esm.sh` and
hydrates client-side.

## Default (start = 0)

```widget:counter-htm
{}
```

## Custom start value

```widget:counter-htm
{"start": "10"}
```
